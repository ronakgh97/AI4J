package com.aiforjava.llm.models;

/**
 * Defines the various features or capabilities that an LLM might support.
 */
public enum ModelFeature {
    TEXT,       // Supports text-based chat
    VISION,     // Supports image input (multimodal)
    THINK       // Supports an internal 'thinking' process
}
